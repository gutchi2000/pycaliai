# EXP07 データ監査 (§5)

**実施日**: 2026-09-20　**方式**: read-only（既存CSV/JSONの列構成・件数・時刻差を確認するのみ、
新規データ収集・学習・API呼び出しは一切なし）。確認できなかった項目は「不明」と明記した。

---

## 5.1 必須入力（判断時点で各候補馬券が持つべき情報）

| 項目 | 入手元 | 状況 |
|---|---|---|
| race_id | `master_v2_*.csv`の`レースID(新/馬番無)`、`data/forward_prices/`, `reports/live_odds/*.json`のファイル名 | あり |
| ticket_type / ticket_key | 単勝/複勝/馬連/ワイド/馬単/三連複/三連単は`pl_probs.py`が全券種の閉形式確率関数を持つ（`p_tansho`, `p_fukusho`, `p_umaren`, `p_wide`, `p_umatan`, `p_sanrenpuku`, `p_sanrentan`）。構成馬の組合せキー生成自体の専用コードはEXP07で新規実装が必要（既存は関数呼び出し時に添字を渡す設計） | 確率計算基盤はあり、ticket_key生成はEXP07で新規実装要 |
| 構成馬 | 候補馬券生成時に添字(i,j,k)として保持可能 | 設計次第(新規) |
| 推定的中確率 | `pl_probs.py`のPL重み(`unified_rank_v6`のスコアから`exp(s_i)`)経由で全券種を閉形式導出可能 | あり(v6 score基盤) |
| 判断時点オッズ | **2026-08-29以降のみ**: `data/forward_prices/YYYYMMDD/*_t10_*.json.gz`(単勝・複勝・ワイド・馬単、真のT-10実測)。**2024-2025は真のT-10データなし**、後述5.4のTANPUKアーカイブのhistorical_pre_snapshot(発走26-30分前)で代替可能だが精度は異なる | 部分的(前向きのみ真値、過去は代替あり) |
| 予測確定オッズ | SettleAI(`analysis/measure_settle_drift.py`)がオッズ帯別の平均倍率テーブルを持つ(点補正、個体分布ではない、n=742) | 粗い代替のみ |
| オッズ下振れ幅 | SettleAIの帯別bootstrap CI幅を転用可能 | 粗い代替のみ |
| 払戻シナリオ | §6の共同着順分布(PL top3 permutation)から導出可能(EXP07で新規実装) | 設計次第(新規) |
| 現行配分 | `compute_bets.py`のtopdownエンジン出力(`reports/`配下)、または`reports/trio_portfolio_shadow_v2/` | あり(比較対象として利用のみ、変更しない) |
| 現行選択順位 | 同上 | あり |
| 判断時刻 | `data/forward_prices/`のtimestamp、または過去分は5.4のTANPUK snapshot時刻 | あり |
| モデルハッシュ | `production_policy.py:sha256_file()` + `data/production_policy.json`の`artifacts`辞書(rank_model, serve_calibrator等のSHA-256) | あり(既存の仕組みをそのまま利用) |

## 5.2 評価時のみ使用

| 項目 | 入手元 | 状況 |
|---|---|---|
| 確定オッズ | `data/kekka_20130105-20251228.csv`(134,840行、2013-2025payoutマスター、cp932) | あり(全期間) |
| 確定着順 | `master_v2_*.csv`の着順列 | あり(全期間) |
| 実払戻 | 同上kekkaマスター + `data/kekka/`週次ファイル(2026年分)、`wide_kekka.csv` | あり |
| 取消・返還 | `master_v2`着順列のNaN/特殊値、および`jvlink_changes.py`系の当日変更情報(2026年分のみ前向き) | 過去分は着順NaNとしてのみ判別可能、取消理由の細別は不明(EXP06監査4.13で既出の限界と同一) |
| 同着処理 | 不明（EXP07の決済ロジック実装時に個別確認が必要。`docs/plans/exhaustive_betting_policy_verdict_20260825.md`は「既存settlerには順位0、返還、1〜3着同着、複数払戻を誤決済する経路があった」と明記しており、**同着・返還の決済は過去に誤りが実際にあった領域**） | 要個別検証、fail-closed設計を踏襲する |

## 5.3 時点安全性

| 確認項目 | 結果 |
|---|---|
| T-35/T-20/T-10のどれを判断時点とするか | **前向き(2026-08-29以降)は真のT-10固定**(`forward_price_protocol.md`)。過去(2024-2025)は後述5.4の通りhistorical_pre_snapshot(発走26-30分前)の代替のみ、真のT-10は存在しない |
| 判断時点より後のオッズ混入 | `forward_price_protocol.md`が「close価格や結果をdecision特徴へ逆流させることを禁止」と明記、既存プロトコルを継承すれば安全。EXP07独自の新規混入経路は候補生成・確率計算いずれも判断時点以前の情報のみで完結する設計とする(§7で担保) |
| 確定オッズを予測値として利用していないか | 現行SettleAIは確定オッズを「点補正の学習対象」としてのみ使用し、判断特徴には使っていない(EXP06期に既検証)。EXP07で新規に確定オッズを紛れ込ませる経路がないことをGate 0で確認する |
| 取消・騎手変更の判明時刻 | 前向き分は`jvlink_changes.py`で当日判明時刻が記録される(2026年分のみ)。過去分(2024-2025)は判明時刻の記録なし、最終的な着順NaNとしてのみ事後判別可能 |
| 確率較正器の学習期間 | `pl_calibrators_v6.pkl`はvalid=2023年でfit(CLAUDE.md記載の時系列分割: train≤2022-12-31, valid=2023, test=2024-01-01〜)。**2024-2025はcalibratorの学習に使われていないことを確認済み**（既存ドキュメントの記述通り、EXP07で新たに検証しない） |
| 候補生成コードの版 | v6本番の`export_weekly_marks.py`/`unified_rank_v6.pkl`のモデルハッシュで固定可能(§5.1)。EXP07独自の候補生成コード(ticket_key構築等)はこれから新規実装するため、当然ながら過去のいかなる時点にも「版」は存在しない(新規実装後にhashを取り、以後固定する) |
| OOF予測または完全OOS予測か | v6のtrain(〜2022)/valid(2023)/test(2024-2025)分割により、**2024-2025のv6スコアは完全OOS**(モデルはこの期間を学習に一切使っていない)。ただしEXP01-06のいずれかで2024-2025を「探索的OOS」として既に開封済みの経緯があるため、EXP07でも同じ位置づけ（探索的、完全未使用ではない）を踏襲し「完全未使用期間」とは呼ばない（絶対条件9） |
| 2024〜2025年学習モデルが混ざっていないか | v6は上記の通り2022年までの学習。ただしSettleAIの帯別補正表(n=742)は**2026-06-07以降のデータのみ**で構築されており、2024-2025評価にはSettleAI補正を使わない(較正なしのodds_floorで代替するか、5.4のTANPUK直接値を使う設計とする) |

## 5.4 過去再現性（2024〜2025年）

**結論: 真のT-10オッズによる過去再現は不可能。現在コードによるreplayとしてのみ評価できる。**

`data/Time _series_odds/TANPUK_20210105-20251228.csv`（区分1=前売り）の各レースにおける
**最終の前売りスナップショット**と、`master_v2`の実際の発走時刻を突き合わせて実測したところ
（2024-2025年、n=6,909レース、race_idで完全一致）:

| 統計量 | 発走前オフセット(分) |
|---|---|
| 平均 | 27.8 |
| 標準偏差 | 1.6 |
| 10%点 | 26 |
| 中央値 | 28 |
| 90%点 | 30 |

すなわち過去データで入手可能な「最も発走に近い」オッズスナップショットは**発走のおよそ26〜30分前
（中央値28分前）であり、T-10ではなくT-28相当**（既存ドキュメントが言う「T-35」帯にほぼ一致、
オフセットのばらつき自体は小さく規則的）。前向き収集（2026-08-29以降）が実現しているT-10取得
（`data/forward_prices/`, `reports/live_odds/`）とは異なる時点であることを明確に区別する。

**したがって**:
- 2024-2025年の評価は「historical_pre_snapshot（発走26-30分前、中央値28分前）のオッズを
  判断時点オッズとして使い、現行のPL確率計算コードと候補生成ロジックを**今のバージョンで**
  当てはめるreplay」としてのみ実施できる。これは**実際にその時点でJRA-VANから配信された
  T-10オッズを使った実配信の再現ではない**（仕様書5.4の要求通り明記。ユーザー指示2026-09-20夜
  により、以後このオフセットを「T-10」「T-28」等のT-記法ではなく`historical_pre_snapshot`
  （`median_minutes_to_post=28`, `range_minutes_to_post=26..30`）と呼ぶ。「T-10 replay」
  「実配信再現」という呼称は使わない）。
- 前向き（2026-08-29以降）の真のT-10データは現時点で8開催日分のみ（`data/forward_prices/`の
  ディレクトリ数）、EXP07のStage 2A/2Bで統計的検出力を持つ規模ではない。

## 【2026-09-20夜、訂正】券種別の実際のデータ状況

前回版は「単勝・複勝・ワイド・馬単に限定（馬連はデータ不在のため除外）」と結論していたが、
これは誤りだった。`data/Time _series_odds/`配下を再確認したところ、以下の通り**馬連には
TANPUKと同一構造の専用historical archive(`UMAREN_*.csv`)が存在し、単勝・複勝と全く同じ
オフセット分布（historical_pre_snapshot、n=6,909レース、median=28分前、range 26-30分前、
完全に同一の測定手法・同一レース集合で実測・確認済み）を持つ**ことが判明した。一方で
**ワイド・馬単には専用のhistorical odds archiveが存在しない**（`build_wide_substrate.py`は
複勝オッズと事後確定payoutからのproxyでワイドを扱っており、真の事前オッズ時系列ではない。
馬単専用ファイルは`data/Time _series_odds/`に存在しない）。

| ファイル | 券種 | ヘッダ | historical_pre_snapshot | 判定 |
|---|---|---|---|---|
| `TANPUK_*.csv` | 単勝・複勝 | あり（清潔） | median=28分前(n=6,909) | データあり |
| `UMAREN_*.csv` | 馬連 | あり（158列=5メタ+C(18,2)ペア） | median=28分前(n=6,909、TANPUKと同一) | **データあり（訂正）** |
| `20210105-20251228.csv`（無接頭辞） | 不明 | ヘッダなし・227列の生ダンプ | 不明 | 現行コード(`build_joint_substrate.py`)から未参照、用途不明のため使用しない |
| ワイド専用ファイル | ワイド | 存在しない | 該当なし | **データなし**（複勝オッズ+事後payoutのproxyのみ） |
| 馬単専用ファイル | 馬単 | 存在しない | 該当なし | **データなし** |

`data/kekka_20130105-20251228.csv`（決済専用、評価時のみ使用）は単勝配当・複勝配当・枠連・
馬連・馬単・３連複・３連単の全列を持つ（§5.2の通り評価時使用は問題ない、判断時点の
オッズ入手可否とは別の話）。

## 券種スコープの最終結論（item 1「有効券種の再監査」を反映）

`compute_bets.py`のCB_ENGINE=topdown経路（486行〜、本番既定エンジン）を直接読み、
`push(...)`呼び出しで実際に候補生成される券種を確認した:

```
単勝(push("単勝",...))  複勝(push("複勝",...))  馬連(push("馬連",...))
ワイド(push("ワイド",...))  馬単(push("馬単",...))
```

**三連複・三連単はtopdown経路のどこにも候補生成コードが無い**（`KIND_ORDER`辞書に表示順の
エントリはあるが、これは券種表示順の定数であり実際の候補生成とは無関係）。CLAUDE.md本文の
「三連単：廃止済み」の記述と一致する。三連複についても現行topdownでは生成されないことが
本監査で確認できた（従来メモリに明記されていなかった追加の事実）。

**Stage 2A主評価の対象は「現在有効（topdown生成対象）」かつ「historical_pre_snapshotの
判断時点オッズが実在する」かつ「Gate J1(時系列安全版)で較正がPASSする」の3条件を
すべて満たす券種に限定する**（Gate J1結果は`CALIBRATION_AUDIT.md`§4参照、
2026-09-20夜追加）:

| 券種 | topdown生成対象か | historical_pre_snapshotデータ | Gate J1(時系列安全版) | Stage 2A主評価 |
|---|---|---|---|---|
| 単勝 | ○ | ○(TANPUK) | PASS | **含む** |
| 複勝 | ○ | ○(TANPUK) | PASS | **含む** |
| 馬連 | ○ | ○(UMAREN) | **FAIL**(H1fit→H2評価で最下位確率帯O/E=1.625、許容上限1.6をわずかに超過) | **除外**(別の探索分析でのみ扱う) |
| ワイド | ○ | ×(proxyのみ) | 未評価(データなしのため対象外) | 除外(別の探索分析でのみ扱う) |
| 馬単 | ○ | ×(データなし) | 未評価(データなしのため対象外) | 除外(別の探索分析でのみ扱う) |
| 三連複 | ×(現行topdownで未生成) | ×(過去データなし、EXP06期未探索領域監査4.14で既出) | — | 対象外 |
| 三連単 | ×(廃止済み) | ×(過去データなし) | — | 対象外 |

**Stage 2A最終対象券種: 単勝・複勝の2券種のみ。** 馬連はdata可用性の観点(item1)では
条件を満たしていたが、較正の時系列安全な検証(Gate J1)で不合格となったため除外した。
この判定は2023 development内で事前固定した基準(期待イベント数10件以上・許容O/E範囲
0.6-1.6)によるものであり、結果を見た後に基準を緩めて救済していない。

- **候補追加は配分改善として扱わない**（item 1指示通り）: 馬連・馬単・ワイドを将来試す
  場合でも、Stage 2Aの主比較（P6 vs P1/P2）には混ぜず、別立ての探索分析として明確に
  分離する。
- 同着・返還の決済ロジックは過去に誤りが実在した領域であり、EXP07独自のsettle実装
  （`evaluate.py`）はfail-closed設計とし、Stage 1合成テストで同着・返還ケースを個別に検証する
  （仕様書8節項目15）。
