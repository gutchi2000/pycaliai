# EXP10 Stage 1 — 設計確定（大敗・中止・能力未発揮リスク）

**作成日**: 2026-09-21　**状態**: Stage 1 設計完了。2024・2025年の性能・ROIは未開封。
本ファイルの数値は全て2023 developmentのみ、または合成データでの検証結果。

主目的変数: **AI◎のモデル期待に対する異常な下方乖離**
（`catastrophic_downside = 1[observed_rank > q90_predicted_rank]`）。

---

## 1. raw PL順位分布の計算法

### 1.1 定義

対象馬（focal horse）の生v6スコア $s_1,...,s_n$（他馬含むレース全体のスコア）から
$w_i = \exp(s_i - \max_j s_j)$。Plackett-Luce の逐次除去表現の下で、
focal馬の着順（rank）は「focalが選ばれるステップ番号(1-indexed)」。

**厳密な同値表現**（Gumbel-max trick, Yellott 1977、リストワイズ学習で標準的に
使われる厳密サンプリング法）: 独立に $g_j \sim \text{Gumbel}(0,1)$ を加え、
$(s_j + g_j)$ の降順で並べた順列は厳密に $PL(w)$ 分布に従う。したがって

$$\text{rank}_i = 1 + \#\{j \neq i : s_j + g_j > s_i + g_i\}$$

この事実により、モンテカルロでのサンプリングは「近似アルゴリズム」ではなく
「厳密分布からの有限標本」であり、誤差は標本サイズ$K$由来の統計誤差のみ
（アルゴリズム自体のバイアスはゼロ）。

### 1.2 三つの計算法（`pl_rank_distribution.py`）

| 手法 | 計算量 | 用途 | 誤差の性質 |
|---|---|---|---|
| **モンテカルロ**(Gumbel-max) | $O(K \cdot n)$、ベクトル化 | **本番**（2023-2025年全レース） | 標本誤差のみ、$K$固定で制御 |
| **厳密bitmask DP** | $O(2^{n-1} \cdot n)$ | 中規模$n$での検証 | ゼロ(厳密) |
| **全順列列挙** | $O(n!)$ | $n \leq 8$のoracleテスト | ゼロ(厳密、数学的定義そのもの) |

bitmask DPの数式（focal以外の$m=n-1$頭をothers、$W_O=\sum_{j \in \text{others}} w_j$）:

$$dp[\emptyset] = 1,\quad dp[S] = \sum_{j \in S} dp[S\setminus\{j\}] \cdot \frac{w_j}{w_f + W_O - W(S\setminus\{j\})}$$

$$P(\text{rank}_f = r) = \sum_{S: |S|=r-1} dp[S] \cdot \frac{w_f}{w_f + W_O - W(S)}$$

### 1.3 モンテカルロの固定パラメータ

- **draw数**: $K=50{,}000$（レースあたり）
- **seed**: `blake2b(f"{global_seed}:{race_id}")` から導出する決定論的整数
  （race_id依存、処理順序に非依存。並列化・再実行しても同一結果を保証）
- **主ラベル用 global_seed**: `20261010`（`labels.py`）
- **1番人気副次診断用 global_seed**: `20261011`（`subsidiary_favorite.py`、主ラベルと
  独立させ、両者が同じ乱数列を共有しないようにした）

### 1.4 近似誤差の実測（合成oracle + 実データ検証）

**合成oracle**（`test_pl_rank_distribution.py`、25テスト全通過）:
- 全順列列挙 vs 厳密DP: $n=3..8$、乱数スコア・一様スコア・極端優劣スコアの
  全パターンで**完全一致**（atol=1e-9）。
- 厳密DP vs モンテカルロ($K=200{,}000$): $n=5,10,16,18$で標準誤差の5σ以内に一致。
- `pl_probs.py`（本番PL厳密計算エンジン）の単勝確率と、本モジュールの
  rank=1周辺確率が**完全一致**（既存本番実装との整合性確認）。

**実データ検証**（`validate_mc_vs_dp_realdata.py`、2023年実レースから$n=5..18$の
各層3レースを層化抽出、計42レース）: モンテカルロ($K=50{,}000$)と厳密DPの
最大絶対誤差は**0.00523**（確率スケール）。この誤差水準で`q90_predicted_rank`
（整数の順位しきい値）が変化したのは42件中2件のみ、いずれも1ランクのみの差
（CDFが0.90にごく近い境界事例）。$K=50{,}000$は本番用途に十分な精度と判断する。

---

## 2. q90ラベルの2023 development実件数

**母集団**: `backtest_pl_ev.score_test(include_valid=True)` の2023年分
（unified_rank_v6、2022年末までの学習で固定、split="valid"、raw score使用・
較正器は一切使わない）。

| 項目 | 件数 |
|---|---|
| 2023年 population中のレース数（頭数3以上） | 3,456 |
| 頭数3未満で除外 | 0（実測、最小頭数は5） |
| tie-break発動件数 | 0（生スコアは連続値のため実務上ほぼ発生しない） |
| 同着(dead heat)で主解析から除外 | 77レース |
| 対象馬の着順がNaN（構造上発生しないはずの検査） | 0 |
| **主ラベル計算対象** | **3,379レース** |
| **catastrophic_downside 陽性数** | **554件（16.40%）** |

**「2023年の陽性数が少なすぎる場合はq90→q80へ緩和せずデータ不足で終了」の判定**:
554件は十分な件数であり（Gate3のfull-control回帰に複数の統制変数を投入しても
過学習しない規模、EXP06/08/09の同種回帰でも同程度〜それ以下の陽性数で実施済み）、
**q90の緩和は不要と判断し、q90=0.90のまま確定する**。

### 参考（構造のみ、副次診断）

- `top3_failure`(既存flop相当) 陽性率: **39.06%**（1,320/3,379）
- `bottom_half`(下位50%) 陽性率: **14.50%**
- 頭数帯別 catastrophic陽性率: 5-9頭=9.09%、10-12頭=17.83%、13-14頭=15.92%、
  15-16頭=17.56%、17-18頭=17.05%（少頭数帯でやや低いが、Gate4での層別確認対象
  として記録するのみ、本Stageでは解釈しない）
- v6 raw勝率(focal_win_prob)五分位別 catastrophic陽性率: Q1低(平均p=0.162)=20.56%
  → Q5高(平均p=0.420)=13.91%（単調減少傾向、v6自身の確信度が高いほど下方乖離は
  減るという自然な関係。B0がこの情報をどこまで吸収するかがGate3の焦点になる）
- `downside_residual`(連続値副次診断) 記述統計: mean=0.0443, median=-0.0327,
  std=0.2442, min=-0.4163, max=0.9454

---

## 3. 対象・除外規則（確定）

1. **評価単位**: 1レース1頭、raw v6 scoreが最大の馬。
2. **tie-break**: `blake2b(f"{TIE_BREAK_SEED}:{race_id}:{血統登録番号}")`の
   決定論的ハッシュが最小の馬を選ぶ。血統登録番号は結果・オッズに依存しない
   馬個体の一意キー。`TIE_BREAK_SEED=20261010`固定。2023年実データでは
   0/3,456件で発動（生スコアがLightGBMの連続出力のため）だが、コードパスは
   合成テストで動作確認予定（Stage2で追加）。
3. **対象馬選択に結果・確定オッズを使わない**: 選択は`_score`（raw v6 score）
   のみに基づく。calibratorも一切ロードしない(`eligible_races.py`は
   `CAL_PKL`/`CURVE_PKL`を変更せず、`score_test()`もraw scoreのみ計算)。
4. **頭数3未満は除外**（実測0件、EXP09の基準を踏襲した安全策として残す）。
5. **同着は主解析から除外**（PLは全順序を前提とするため）。2023年で77レース
   （2.23%）。EXP09の「同着2023=3件」という既存記録とは**定義が異なる**点に
   注意: EXP09は単勝/複勝/馬連の**払戻対象馬番の同着のみ**を数えたのに対し、
   本監査は**フィールド内のどの着順にでも重複数値があれば**除外している
   （例: 10着同着2頭、のような下位の同着も含む）。この定義差は意図的（PLの
   前提が壊れるのは順位のどこであれ重複があれば同じであるため）であり、
   バグではない。
6. **取消・除外・中止でv6スコアが存在しない馬**: master_v2に行自体が存在しない
   ため、対象馬選択の母集団から構造的に除外される（DATA_AUDIT.md §7で確認済み）。
   件数は§7参照、主解析には混ぜない。

---

## 4. 特徴の時点安全監査

### 4.1 B0〜B3の統制変数

| モデル | 追加変数 | ソース | 時点安全性 |
|---|---|---|---|
| B0 | v6自身の下方尾部確率（predicted_rank_distの分布形状統計、例: P(rank>中央値)、score_std等） | `pl_rank_distribution.py`のMC/DP出力（事前計算可能、結果不要） | ✅ 予測時点で計算可能 |
| B1 | B0 + 市場確率・人気順位 | 外部kekkaファイル`単勝オッズ`（発走前最終オッズ、EXP06/EXP09の`市場確率`comparatorと同一ソース） | ✅ 発走前オッズ、TARGET frontier由来の historical dataset（JRA-VANのpublic-posting制限は対外公開の話であり内部研究利用は既存EXP06/07/08/09で確立済み） |
| B2 | B1 + field size・max probability・entropy・score gap | v6 raw score由来（`n`,`p1_max`,`entropy`,`gap_12`。`diag_upset_decomposition.py`と同じ計算式） | ✅ 予測時点で計算可能（v6自身の出力のみ） |
| B3 | B2 + 出走回数・休養日数 | master_v2既存列（`kako5_race_count`または`休み明け～戦目`、`間隔`） | ✅ 既存v6予測特徴、確立済み安全 |
| R1 | B3 + 下方リスク専用特徴（下記4.2） | 下記参照 | 個別に監査（下記） |

### 4.2 R1専用特徴の時点安全監査（ユーザー指定10候補）

**現在レースの着差・上がり・通過順位・馬体重結果はいかなる特徴にも使わない**
（全て「過去の」接頭辞つきの集計のみ）。

| 候補 | 状態 | ソース／構築方法 |
|---|---|---|
| 過去着順の分散 | ✅ **既存列で対応可** | `kako5_std_pos`（既にmaster_v2に存在、v6予測特徴として使用中＝安全性確立済み） |
| 過去走破タイム残差の下方分位 | 🔧 **要構築** | EXP08の`expected_time_model.py`（expanding-window設計、テスト済み資産）を馬単位でローリング適用し、過去5走の残差の下位分位を計算する。新規結合が必要 |
| 過去の大敗頻度 | 🔧 **要構築（2案）** | (a) `kako5_upset_good_count`/`kako5_hidden_good_count`（既存列、ただし定義が「不利により地力を発揮できなかった」に近く、大敗の定義とは微妙に異なるため要個別確認）、(b) 本実験自身の`catastrophic_downside`ラベルを対象馬の過去レースに再帰適用（常に厳密に過去のレースのみを参照するため時点安全、循環参照にならない） |
| 好走と大敗の振幅 | ✅ **既存列で対応可** | `kako5_best_pos`と`kako5_avg_pos`の差、または`kako5_std_pos`で近似可能 |
| 休養明け成績の分散 | 🔧 **要構築** | 「間隔が一定閾値以上だった過去レース」に限定した着順分散を馬単位で集計。新規結合が必要 |
| 距離・馬場・競馬場変更 | ✅ **既存列で対応可** | `前距離`vs`距離`、`前走馬場状態`vs`馬場状態`、`前走場所`vs`場所`の差分フラグ（全て既存列、単純な比較で構築可） |
| 馬体重および馬体重変化の履歴変動 | 🔧 **要構築（要注意）** | `前走馬体重`/`前走馬体重増減`は単一の直近値のみ既存。複数過去走にわたる変動幅の集計は新規構築が必要。**馬体重の内部利用自体はv6が`前走馬体重`を既に予測特徴として使用しており前例あり**（[[project_jravan_guideline_compliance]]の撤去対象は「サイトへの生値の公開」であり内部モデル利用を禁じるものではない、と確認済み） |
| 出遅れ・位置取り悪化の過去頻度 | 🔧 **要構築** | `prev_pos_rel`/`closing_power`(既存、直近1走のみ)を馬単位でローリング集計し「過去に位置取りが悪化した頻度」を作る。新規結合が必要 |
| 騎手・調教師変更 | 🔧 **要構築（軽度）** | master_v2に「前走騎手コード」列は存在しない。`前走レースID(新/馬番無)`で自己結合し前走の騎手コード・調教師コードを取得、現在行と比較するフラグを作る（単純な自己結合、リスク低） |
| 過去DNF履歴（時点安全に作れる場合のみ） | 🔧 **要構築（コスト高）** | master_v2にDNF情報がない（DATA_AUDIT §7）。外部ソース（`add_20130105-*.csv`または外部kekkaファイル）を血統登録番号でas-of結合し、現在レース日より**厳密に前**のDNF件数を集計する必要がある。実装コスト・データ結合リスクが他候補より高いため、ユーザーの「時点安全に作れる場合のみ」という条件に従い、**Stage2では優先度を最後に置く**ことを提案する |

**時点安全性の結論**: 10候補中4つは既存列で即座に対応可能（過去着順分散・
振幅・距離変更・馬場/競馬場変更）、5つは既存の安全な過去データから新規構築
可能（低〜中リスク）、1つ（過去DNF履歴）は外部ソース結合が必要で最もコストが
高い。いずれも「今回のレースの結果」を一切参照しない設計であり、全て
予測時点で計算可能（項目10の結論と整合）。

---

## 5. 既存flop detectorとの等価性表

`diag_flop_detector.py`のラベル（`flop = 1 - hon_top3`、◎が複勝圏外）を、
`labels.py`で計算済みの`top3_failure`列（定義は同一: `observed_rank > 3`）として
**同じ2023年対象行**（3,379レース）で再現し、`catastrophic_downside`との
関係を実測した:

| | catastrophic=0 | catastrophic=1 | 計 |
|---|---|---|---|
| **top3_failure=0(既存flop=0)** | 2,059 | 0 | 2,059 |
| **top3_failure=1(既存flop=1)** | 766 | 554 | 1,320 |
| **計** | 2,825 | 554 | 3,379 |

**中心的な発見**: `catastrophic_downside=1`の**100%**が`top3_failure=1`
（既存flopのサブセット）である一方、`top3_failure=1`のうち`catastrophic_downside=1`
なのは**41.97%**のみ。単純一致率77.3%。

**解釈**: `q90_predicted_rank`は3,379レース中95.9%で3より大きい（中央値6、
平均6.2）。これは、v6自身のPL分布上、多くのレースで◎が4-6着に終わる確率が
10%を優に超えている（＝v6自身、◎が僅差の混戦の中の1頭に過ぎないと"知っている"
場合が多い）ことを意味する。既存の`flop`（固定閾値=複勝圏内/外）は「v6が
どれだけ自信を持っていたか」を無視した粗い基準であり、**v6が最初から4-6着も
十分あり得ると予想していた"順当な"複勝圏外」と、"v6自身の予測分布からみても
まず起こらないはずだった異常な下方乖離"を区別できない**。`catastrophic_downside`
はこの2つを分離し、後者（真に驚くべき失敗）だけを陽性とする、既存flopの
**厳密な部分集合**として設計されている。

**B2との対応**: `diag_flop_detector.py`のGate10/11特徴（`entropy`,`gap_12`,`n`,
`baba_n`,`score_std`）は、本設計のB2（field size・max probability・entropy・
score gap）とほぼ1対1に対応する。したがって「B2 vs R1のfull control」は、
実質的に「既存flop detectorの統制セットに新ラベル・新特徴を通した再検定」に
等しく、ユーザー指示（項目7）の「B2相当の比較対象として扱う」を満たす。

**PRIOR_ART_AUDIT.mdの訂正**: Stage0時点では「候補5（高top3確率馬の複勝圏外）
は既存研究との強い重複」と評価したが、Stage1の実測により、**主ラベルの
catastrophic_downsideは既存flopラベルとは異なる(厳密な部分集合の)ラベルであり、
「既存研究の言い換え」ではなく「既存研究が識別できなかった内訳の分離」**
であることが確認された。重複度の評価を「強い重複」から「ラベル定義が異なる
新規性あり、特徴セットのみB2相当で重複」へ訂正する。

---

## 6. モデル B0〜B3 / R1（仕様）

全モデルは同一の目的変数`catastrophic_downside`を予測する二値分類器として
指定する（ロジスティック回帰を既定、EXP06/08/09との手法整合性のため。
非線形性が疑われる場合はGate3の頑健性確認としてGBM版も併記可、ただし
**主判定は線形回帰の係数CI**とする、EXP09のGate3設計を踏襲）。

| モデル | 特徴集合 | 目的 |
|---|---|---|
| B0 | v6自身の下方尾部確率（predicted_rank_distから導出、1変数） | 「ラベルの定義に使った情報だけで、ラベル自身をどこまで説明できるか」の下限点検 |
| B1 | B0 + 市場勝率・人気順位 | 市場情報を加えた場合の上乗せ |
| B2 | B1 + n・max_prob・entropy・gap_12 | 既存flop detector相当の統制（§5参照） |
| B3 | B2 + 出走回数・休養日数 | EXP02の確立知見を反映した最終統制セット |
| R1 | B3 + 下方リスク専用特徴（§4.2） | **主眼**。B3を完全統制した後の固有上積みを検定する対象 |

**主比較は R1 vs B3**（ユーザー指示通り）。B0→B1→B2→B3は補助的な内訳確認
（Gate2相当の段階的比較、EXP09と同型）として保持するが、最終判定を左右する
のはB3→R1の一段のみ。

---

## 7. 最小反証実験（Stage 1.5、2024/2025を開封しない早期終了ゲート）

Stage2のフル実装（B0-R1全ての構築＋Gate0-5実行）に進む前に、**2023年development
データのみ**を使った$k$分割交差検証（$k=5$、race単位、meeting-day層化）で、
R1専用特徴がB3に対して**in-sample/CVで最低限の限界的寄与**を持つかを検査する。

- 手順: 2023年3,379レースを5分割。各foldでB3とR1を学習、held-out foldでの
  deviance/partial $R^2$の差を計算、5fold平均を報告。
- **停止基準**: 5fold平均のR1-B3 deviance改善が実質ゼロ（事前登録閾値: 標準偏差
  換算で無視できる水準、具体的な数値閾値はStage2実装時に凍結）の場合、
  **Stage2のフル実装（2024/2025のGate3評価）に進まず、ここで終了する**。
- この検査は2023年のみで完結し、2024・2025年のデータを一切必要としない
  ため、「2024・2025年の性能・ROIをStage1で開封しない」という制約の範囲内で
  実施できる、最も安価な早期終了点である。

---

## 8. 中止条件（確定）

1. Stage1.5（§7）でR1がB3に対しCV上の限界的寄与を持たない場合、Stage2へ
   進まず終了する。
2. Stage2まで進んだ場合、**Gate3（R1 vs B3のfull control、2024・2025年）**が
   主判定。logloss差・Brier差の97.5%CI上限が共に0未満、かつPR-AUCが改善、
   かつ年度・競馬場・人気帯・芝ダートの一部区分に集中していないことが必要。
   いずれかが満たされない場合はFAILとし、**統制変数を減らす・q90を変更する・
   参加率を変える等の条件変更による救済は行わない**（EXP06/08/09と同一規律）。
3. Gate3がFAILした場合、経済評価（§8のGate、投資縮小/見送りの2案）は実施しない。
4. q90=0.90、tie-break規則、MC draw数(K=50,000)・seed、同着除外規則は、
   2024・2025年の結果を見た後に変更しない。
5. 2023年の陽性数（554件）は十分と判断済みのため、q90→q80への緩和は
   いかなる段階でも行わない。

---

## 9. 合成oracleテスト設計（実施済み、`test_pl_rank_distribution.py`）

| テスト対象 | 手法 | 結果 |
|---|---|---|
| 厳密DP vs 全順列列挙 | $n=3..8$、乱数/一様/極端優劣スコアの各パターン | **完全一致**（25テスト中該当分全通過） |
| モンテカルロ vs 厳密DP | $n=5,10,16,18$、$K=200{,}000$、5σ許容 | **通過** |
| MCの決定論性 | 同一seed・race_idで2回計算し完全一致を確認 | **通過** |
| MCのrace_id非依存性 | 処理順序を入れ替えても同一race_idなら同一結果 | **通過** |
| 分布の基本性質 | 合計1、非負、一様分布の期待値=(n+1)/2 | **通過** |
| 既存`pl_probs.py`との整合性 | rank=1周辺確率 = 標準PL単勝確率 | **通過**（$n=10$、全馬で確認） |
| 分位点関数(`quantile_rank`) | 手計算ケース・退化分布（確実1着）での境界確認 | **通過** |

合計25テスト、全通過（`python -m pytest test_pl_rank_distribution.py`）。
さらに実2023年データ42レース（$n=5..18$層化抽出）でモンテカルロと厳密DPを
突合し、最大誤差0.00523（確率スケール）・q90閾値の差は最大1ランク
（42件中2件のみ）であることを確認した（§1.4）。

---

## まとめ（Stage 1 完了時点）

- 主ラベルの計算法・2023実件数（554/3,379=16.40%）・対象/除外規則を確定した。
- R1専用特徴10候補の時点安全性を監査、4つは既存列、5つは低〜中リスクで構築可能、
  1つ（過去DNF履歴）は高コストのため優先度を下げる。
- 既存flop detectorとの等価性を実測し、catastrophic_downsideがflopの厳密な
  部分集合であることを確認、Stage0の「重複」評価を訂正した。
- B0-R1のモデル仕様、Stage1.5の最小反証実験、中止条件を確定した。
- oracleテスト（合成25件+実データ42件検証）でエンジンの正しさを確認した。
- **2024・2025年の性能・ROIは一切開封していない**。

関連: [[project_exp10_downside_risk]] [[project_loss_is_anchor_prediction]]
[[project_mcond_exp02_dynamic_skill]] [[project_mcond_exp06_jev_decision]]
[[project_exp08_online_track_state]] [[project_exp09_conformal_abstention]]
[[project_jravan_guideline_compliance]] [[project_kekka_ext_data_quirks]]
