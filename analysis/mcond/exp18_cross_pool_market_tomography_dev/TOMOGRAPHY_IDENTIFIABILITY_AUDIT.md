# EXP18 Stage 0 — tomography 識別性監査（複勝 Lo/Hi 逆算・T2）

**凍結仕様**: v0.4-frozen-stage0（commit `67b874ec`）
**実装**: `payout_formula.py`（式と逆算）/ `fukusho_roundtrip.py`（実データ往復）/ `tomography.py`（T2）
**本節 §1〜§5 は実データの往復を実行する前に固定した**（結果は §6 に後から追記する）。

---

## 1. 目的

T2_SOFT_FUKUSHO_MAXENT は複勝市場の「各馬の top-k 入り確率」を soft 制約として使う。
表示されている複勝オッズは Lo/Hi の**範囲**であり、確率そのものではない。表示から
複勝プールの票シェアを逆算し、それを同じ式で表示へ戻したときに表示を再現できるかを確かめる。
再現できなければ、表示から作る制約は式の誤り・丸め・返還処理のいずれかで汚れているので、
**T2 は未実装として停止する**（別の関数形で救済しない）。

## 2. 使用する JRA 複勝払戻式（実行前に固定）

| 項目 | 固定した内容 |
|---|---|
| 純プール | `D = (1 − 0.20) · V`（V = 複勝プール総票数。控除率 20%） |
| 的中票控除 | 払戻対象 k 頭 W の元本 `Σ_{j∈W} V_j` を D から差し引く |
| `places(n)` による分配 | 残り `D − Σ_{j∈W} V_j` を k 頭で**等分**する。1 票あたり利益 = `(D − Σ_{j∈W} V_j) / (k · V_i)` |
| オッズ | `odds_i(W) = 1 + (D − Σ_{j∈W} V_j) / (k · V_i)` |
| 表示 Lo | 他の k−1 頭が**票数上位**（最も人気）の場合 = 最小値 |
| 表示 Hi | 他の k−1 頭が**票数下位**（最も不人気）の場合 = 最大値 |
| 10 円 breakage | 表示は **0.1 倍単位の切り捨て**（払戻の 10 円単位切り捨てと同値）。下限 1.0（元返し） |
| `places(n)` | 出走（starter）8 頭以上 → k=3、5〜7 頭 → k=2（4 頭以下は複勝発売なし） |
| starter | 確定単勝オッズ > 1.0 を持つ馬番（EXP16A と同定義）。締切前取消は pool に居ない |
| 取消・返還 | 締切前取消は starter から除外する。締切後の返還は表示に現れないので往復では扱わない |

シェア `v_i = V_i / V` で書けば式は票の尺度に依存しない（0 次同次）ので、総票数列は使わない。

## 3. 逆算の数値解法（式とは別。式を変えない）

人気順（表示 Lo、同値は Hi の昇順）を固定すると、各馬の「他の上位 k−1 頭」「他の下位 k−1 頭」が
決まり、丸め前オッズが切り捨て区間 `[表示, 表示+0.1)`（表示 1.0 は下側を課さない）に入る条件は
シェア v について**線形不等式**になる:

`v_i·(k(L−1)+1) ≤ (1−t) − Σ_{j∈C_i} v_j < v_i·(k(U−1)+1)`

Σv=1・v>0・人気順の単調性を加え、全不等式の最小余裕を最大化する LP（HiGHS）で内点を取る。
表示が完全に同じ馬（tie）の順序は表示から決まらないので、tie 群の中の順列（最大 64 通り）を試し、
再生成が一致したものを採る。

**合成検査**（式が正しい世界で表示を作り、逆算して戻す）: 1,000 race で馬行一致率 **100.0%**、
race 全馬一致 **100.0%**、1 race あたり 2.6 ms（`test_invariants.py` の
`place_formula_synthetic_roundtrip` にも組み込み）。したがって実データで不一致が出れば、
それは**解法ではなく式（または表示データ）の不一致**である。

## 4. 往復 Gate（実行前に固定）

| 項目 | 内容 |
|---|---|
| 対象 snapshot | **terminal_close_market（区分4）**。historical_pre_snapshot は参考として別集計 |
| 対象 race | base race set（平地・starter ≥ 5・全 starter が単勝と複勝 Lo/Hi を持つ） |
| 対象年 | 2013〜2023（市場構造 loader。表示だけを使い、着順・払戻は読まない） |
| 馬行の合否 | Lo と Hi の**両方**が `|再生成 − 表示| ≤ max(0.05, 0.02 × 表示)` |
| Gate | **各年**の馬行合格率 ≥ **99%** |
| 未達時 | **T2 は未実装として停止**。UB3 は Stage 1 の Gate arm に入らない。別の関数形で救済しない |
| 構成 assert | `Σ_i τ_i = places(n) ± 1e-6`（τ の定義から構成的に成立） |

## 5. τ と T2 の単一形（spec どおり。通過時だけ実装）

- **τ の定義（宣言）**: 逆算したシェアから `τ_i = places(n) · v_i`。Σ τ = places(n) を構成的に満たす。
  これは「複勝プールの票シェアを k 倍したもの」という宣言した写像であり、真の top-k 確率だとは主張しない。
- **T2**: 事前 = T1（λ 割引 Harville）の順序付き top-k 分布。単勝 marginal は**等式制約**
  （先頭馬ごとのブロック再規格化で厳密に満たす）、複勝は `w_fuku · Σ_i (P_q(i∈top-k) − τ_i)²` の
  単一ペナルティ形、`k = places(n)`、`w_fuku ∈ [0,1]`。KL(q‖q_T1) + ペナルティを最小化する。
  `w_fuku = 0` は T1 を同じ計算でそのまま返す（bit 一致、`T2_w0_equals_T1_bitwise`）。
- **Stage 0 の dry-run fit**（通過時だけ）: λ は T1 の ≤2018 fit 値に固定し、
  `w_fuku ∈ {0, 0.1, …, 1.0}` を 2016〜2018 の realized 順不同 top2 logloss（結果 loader、≤2018）で選ぶ。
  2019 年以降の結果は使わない。

## 6. 結果

`fukusho_roundtrip.py` → `out/fukusho_roundtrip.json`（着順・払戻は読んでいない。表示オッズだけ）。

### 6.1 往復 Gate: **FAIL** → **T2_unimplemented_stop**

| 年 | race | 馬行 | 合格馬行 | **馬行合格率** | race 全馬合格 | 最大 \|誤差\| Lo / Hi |
|---|---|---|---|---|---|---|
| 2013 | 3,324 | 48,293 | 9 | **0.02%** | 0 | 128.6 / 137.8 |
| 2014 | 3,326 | 48,560 | 52 | **0.11%** | 0 | 95.6 / 129.8 |
| 2015 | 3,326 | 48,240 | 110 | **0.23%** | 0 | 74.5 / 134.5 |
| 2016 | 3,326 | 48,331 | 100 | **0.21%** | 0 | 73.5 / 140.3 |
| 2017 | 3,329 | 47,585 | 88 | **0.18%** | 0 | 66.5 / 112.0 |
| 2018 | 3,328 | 46,869 | 106 | **0.23%** | 0 | 91.7 / 161.3 |
| 2019 | 3,325 | 45,777 | 118 | **0.26%** | 0 | 73.7 / 125.6 |
| 2020 | 3,331 | 46,564 | 111 | **0.24%** | 0 | 71.9 / 147.4 |
| 2021 | 3,329 | 46,168 | 125 | **0.27%** | 0 | 72.0 / 123.4 |
| 2022 | 3,330 | 45,645 | 118 | **0.26%** | 0 | 76.8 / 149.8 |
| 2023 | 3,329 | 46,039 | 115 | **0.25%** | 0 | 77.4 / 182.8 |

Gate は各年 99% 以上。実測は全年 0.3% 未満（最小 2013 年 0.02%）で、どの年も大きく届かない。
historical_pre_snapshot（参考）も全年 0.1〜0.3% で同じ結果。

- 表示 Lo 帯別（terminal、2013–2023）: <2 倍 0.81%、2〜5 倍 0.02%、5〜20 倍 0%、20 倍以上 0%。
- places 別: k=2（5〜7 頭）1.33%、k=3（8 頭以上）0.20%。
- 表示に完全 tie を含む race 10,870（tie 群内の順列は最大 64 通り試した）。

### 6.2 不一致の形（記述だけ。式・Gate は変えず、別の関数形は試していない）

| 指標（terminal、518,071 馬行） | 値 |
|---|---|
| 再生成 Lo が表示より低い / 高い | 89.1% / 1.4% |
| 再生成 Hi が表示より低い / 高い | 16.3% / 76.4% |
| 相対誤差の中央値 Lo / Hi | −10.3% / +5.6% |
| Hi/Lo 比の中央値: 表示 / 再生成 | 1.59 / 1.86 |

固定した式でシェアを逆算して戻すと、**幅が表示より系統的に広く**（Lo が低く Hi が高く）出る。
合成データ（式が正しい世界）では同じ逆算が 100% 一致するので（§3）、これは解法の誤差ではなく、
**§2 の式が JRA の表示 Lo/Hi の算出方法を再現していない**ことを意味する。どの規則が違うのか
（分配・範囲の定義・丸め・特例のいずれか）は特定していない。特定するには別の関数形を当てることになり、
それは spec が禁じる救済にあたるため行わない。

### 6.3 帰結

- **T2_SOFT_FUKUSHO_MAXENT は未実装で停止**。τ を実データから作らず、w_fuku の dry-run fit も行わない。
- **UB3_OFFSET_CONDITIONAL は Stage 1 の Gate arm に入らない**。Gate arm は UB2 の 1 本（Bonferroni 係数 1）。
- `T2−T1` 頭対頭と `UB3−UB2` decomposition は算出不能として報告する。
- `tomography.t2_soft_fukusho` と合成テスト（w=0 で T1 と bit 一致、単勝等式、解析解の一致）は
  コードとしては残すが、実データの制約には一度も使っていない。
- 別の複勝式で T2 を救済することは、この EXP18 では行わない（行うなら新番号・新 spec で、式を実行前に固定する）。

### 6.4 将来の別実験候補（v0.5 で記録のみ）

≤2018 の実複勝配当から払戻式そのものを先に識別し、その式で往復 Gate を固定し直す案だけを記録する。EXP18 では実施しない
（T2 は未実装のまま、UB3 は実行しない、別の払戻式は試さない）。
