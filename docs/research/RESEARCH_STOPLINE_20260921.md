# 研究停止線 (2026-09-21)

**位置づけ**: 「予測AI全体が不可能」という結論ではない。**現在のJRA表データと
既存モデル基盤（unified_rank_v6 + 市場データ）で、追加実装の合理的根拠が
ある主要仮説を使い切った**、という状態整理。EXP01〜EXP12（条件付き検定
基盤を持つ体系的な敵対的検証シリーズ）が、それぞれ独立に事前登録した
Gate・placebo・full-control検定を通じて収束的にこの結論へ至った。

これにより、既存表データを使った大型予測研究（EXPシリーズ）を一時停止する。
**EXP13以降は開始しない**。次の研究段階は「研究実行」ではなく「観測・
再開条件の整理」（本ドキュメント自体がその一部）。

---

## 0. ★2026-09-21、ユーザー判断により研究再開（本節を追記）

2026-09-21、venue06収集障害の修正（commit `73c4c7f2`/`6bc06bc1`、ユーザー
承認済み）完了後、**ユーザーの明示指示により未検証研究を再開する**。
「EXP13以降は開始しない」という上記§1原則は撤回され、**EXP13
「出走後の中止・非完走リスク」のStage 0監査から着手する**。

再開にあたり、以下の既存規律は**そのまま維持**する（撤回しない）：
- 本ドキュメント§2〜§4の「失敗した正確な仮説」「未検証として残る範囲」
  「再実験禁止項目」は無効化されない。EXP01〜EXP12個別の再走禁止・
  条件変更禁止は引き続き有効。
- 2024・2025年データの開封は、個別実験の設計で明示的に許可されない限り
  禁止のまま（EXP13 Stage 0は監査のみで、2024/2025性能開封は行わない）。
- `feedback_asof_population_definition`（as-of母集団定義の必須要件）等、
  既存のプロジェクト全体メソドロジー規律は全て継続適用。
- EXP01〜EXP12の「完全終了」ステータス自体は変わらない。再開は
  「新しい未検証範囲（EXP13）への着手」であり、既存EXPの蒸し返しではない。

EXP13以降の各実験も、これまでと同じStage 0（監査）→Stage 1（実装・
minimal falsification）→Gate評価（2023年内開発データのみ）→結論、という
ライフサイクルに従う。

**EXP13「出走後の中止・非完走リスク」完全終了**（2026-09-22、
`analysis/mcond/exp13_nonfinish_risk_dev/`）。Stage 0監査完了後、
Stage 1のモデル学習前段階としてGate 0A〜0D（時点安全な評価基盤の
再構築可否）を実施し、**Gate 0でデータ不足終了**した。実装・学習・
バックテスト・2024/2025年性能開封・ROI評価は一切行っていない。

> EXP13は非完走リスクモデルの性能FAILではない。2013〜2025年の結果ラベルと
> 事前特徴は存在するが、development期間である2023年に判断時点市場snapshotが
> 存在せず、v6＋市場を統制した主仮説を時点安全に検証できないため、Gate 0で
> データ不足終了した。

Gate 0A（市場オッズ取得時点）で、2023年に判断時点market snapshot
（historical_pre_snapshot）が構造的に0%であることが確定し、市場統制なしの
比較では主仮説を検証できないためS1以降は実装しなかった。Gate 0D（2023年
145陽性・EPV約11・事前効果量なし）は単独では中止理由でないが、Gate 0Aの
構造的欠損と合わせ継続根拠不足と判断。詳細・再開条件・再利用可能資産は
`analysis/mcond/exp13_nonfinish_risk_dev/README.md`・`spec.json`参照。

**EXP14「レジーム分離・Mixture of Experts」Stage 0完了**（2026-09-22、
`analysis/mcond/exp14_regime_moe_dev/`）。実装・学習・バックテスト・
2024/2025年性能開封・ROI評価は一切行っていない（既存成果物の引用のみ）。
主仮説「事前情報だけで選択した専門家モデルがpooled modelより未使用期間で
良い」について、**手動条件分割は既に3件の独立実験（distance-band・
surface・season×venue）で収束的に否定済み**であることを確認した
（`train_expert.py`・`exp_surface_split.py`・[[project_summer_specific_
model_dead]]）。v6（515本・深さ12のLightGBM）の条件特徴は個々のgainは
低いがsplit回数は多く、tree構造が既にregime的交互作用を捉えている可能性を
新規に実測した。未踏査領域（venue単独・クラス・年齢・非手動gating手法）は
残るが、芝・ダート軸は`exp_surface_split.py`が既に2024-2025年を消費済み。
詳細は`analysis/mcond/exp14_regime_moe_dev/README.md`参照。**Stage1
（実装）はユーザー報告・承認後に着手する**。

---

## 1. EXP01〜EXP12 状態一覧

| EXP | テーマ | 状態 | 主判定 |
|---|---|---|---|
| EXP01 | 市場条件付き検定基盤 + 陣営選択の逸脱 | 完全終了 | Gate2 FAIL（逸脱固有の価値なし、経済価値なし） |
| EXP02 | 動的対戦能力（Weng-Lin PL） | 完全終了 | 主Gate PASS（固有情報あり、1着にも有意）だが経済価値なし。効果の3/4は出走回数・休養日数という生の事実 |
| EXP03 | 恒常能力・短期状態の分離 | 完全終了 | Gate2主判定FAIL |
| EXP04 | 環境安定性選別（陣営選択の頑健性フィルタ） | 完全終了 | 追加情報はあるがフィルタ自体が有害（通常学習に負ける） |
| EXP05 | 市場残差（表特徴の追加情報） | 完全終了 | 情報は頑健に実在するがROI非有意、情報圧縮修正は無価値 |
| EXP05-F | EXP05の前向き影武者観測 | **前向き観測稼働中**（結果非開封） | 2026-09-19開始、目標6600レース中39件（本ドキュメント§3で収集完全性のみ報告） |
| EXP06 | Jev（LLM判断層）の意思決定検定 | 完全終了 | 3分類（direct decision/full-control固有情報/downstream特徴）全てFAIL |
| EXP07 | ロバストポートフォリオ最適化（P6 CVaR） | 完全終了 | Gate1 FAIL（方向不一致・CI0跨ぎ・利益が大穴集中） |
| EXP08 | 当日馬場状態オンライン推定（Kalman） | 完全終了 | Gate2A/2B FAIL（効果量が閾値の4-6桁小さい、permutation placebo不合格） |
| EXP09 | Conformal/OODによる保証付き見送り | 完全終了 | coverage自体は再現、but abstention固有の価値なし（Gate3 full control後に消失） |
| EXP10 | 大敗・中止・能力未発揮リスク | 完全終了 | 既存の時点安全な表特徴＋完走馬限定headはStage1.5でB3を上回らず（範囲限定、§2参照） |
| EXP11 | 階層ベイズによる疎データ・未知条件への外挿 | 完全終了（モデル実装前に仮説撤回） | 選定根拠の62%過大予測が母集団選択バイアスのアーティファクトと判明、as-of訂正で消失 |
| EXP12 | 対戦ネットワークによる対戦相手の質 | 完全終了 | O3（対戦相手identity追跡）はplacebo検定で決定的FAIL |

---

## 2. 失敗した正確な仮説（各EXPの結論の範囲に厳密に限定、一般化しない）

- **EXP01**: 調教師・騎手・馬の「通常と異なる選択」（陣営選択の逸脱）は、
  生の選択事実（間隔・距離変更等）を統制した後、固有の予測価値を持たない。
- **EXP03**: 馬の恒常的能力と短期的状態を分離するモデルは、単純な直近
  成績統計を上回る予測価値を持たない。
- **EXP04**: 「環境が安定した条件のみで学習する」という選別フィルタは、
  フィルタなしの通常学習より予測精度が悪化する。
- **EXP06**: LLM判断層（Jev）による直接的意思決定・完全統制後の固有情報・
  下流特徴としての活用、いずれも予測/判断価値を持たない。
- **EXP07**: ロバスト最適化（CVaRベースのP6ポートフォリオ）は均等配分
  （P1）に対し優位性を持たない。
- **EXP08**: 当日の時計・上がり・ペースのオンライン推定（Kalman状態空間
  モデル）は、v6+市場の後に追加情報を持たない（内外・脚質と合わせ
  「同日状態」という4次元全てが閉じた）。
- **EXP09**: Conformal予測によるカバレッジ保証を「見送り判断の質」へ転用
  しても、モデル自身の基本的確信度（最大確率・エントロピー）を統制すると
  固有価値が消える。
- **EXP10**: 既存の時点安全な表特徴だけを用いた、**完走馬に対する**AI◎の
  catastrophic downside専用headは、2023年内の時系列CVでB3（v6確率＋市場＋
  頭数＋エントロピー＋出走回数＋休養日数の統制モデル）を上回らなかった。
  DNF・中止・取消・除外の別母集団、raw結果履歴から新規構築する過去大敗
  頻度、着順・走破タイム残差の個体内分散、位置取り崩壊頻度、故障・疾病
  理由を使うモデル、完走確率そのもの——これらは**未検証**（§3参照）。
- **EXP11**: 低頻度騎手×調教師ペアという疎データ領域選定の根拠だった
  「v6予測0.059対実現0.036」という62%の過大予測は、母集団を全期間集計
  （未来を含む）で定義したことによる選択バイアスであり、as-of訂正すると
  消失する。**階層ベイズという手法自体の性能FAIL"ではない"**——
  モデル実装前に仮説を撤回した。
- **EXP12**: 個々の過去対戦相手を対象日まで追跡した1-hop特徴（O3）は、
  2023 meeting-day CVで小さな改善を示したが、degree・年齢・経験量を
  維持したplaceboを上回らず、対戦相手identity固有の情報とは確認できな
  かった。**PageRank・2-hopグラフ・graph embedding・GNNは「失敗」ではない**
  ——未実装・未検証であり、O3がGateを通らなかったため実装根拠なしとして
  保留する。

---

## 3. 未検証として残る範囲（実施しない、別仮説として保留）

EXP10由来:
1. DNF・中止・取消・除外の別母集団（完走馬限定の各実験のスコープ外）
2. raw結果履歴から新規構築する過去大敗頻度
3. 着順・走破タイム残差の個体内分散
4. 位置取り崩壊頻度
5. 故障・疾病理由を使うモデル（構造化データが存在しないことのみ確認済み）
6. 完走確率そのもの

EXP11由来:
7. 階層ベイズ一般
8. partial pooling一般
9. 新規騎手・新規調教師への縮約
10. 種牡馬・母父の階層モデル
11. Q2/Q3人気帯における低頻度pair interaction（探索的発見、未検証）

EXP12由来:
12. PageRank（O4）
13. 2-hopグラフ（O4）
14. graph embedding（O5）
15. GNN（O6）

**これらをEXP01〜EXP12の各実験へ追加して再検証することは禁止する**。
実施する場合は必ず新規の独立した実験として着手すること。

---

## 4. 再実験禁止項目（「再走禁止」と明記されたもの）

- EXP07: P6ロバストCVaRポートフォリオ最適化
- EXP08: 当日馬場状態オンライン推定（時計・上がり・ペース・内外・脚質の
  4次元、Kalman状態空間モデル全般）
- EXP09: Conformal/OODを独立した参加判断器として使う設計、EXP06 Jevと
  同型の「モデル自身の確信度を統制すると消える」パターンの再検定
- EXP10: 完走馬限定・既存時点安全表特徴のみによるcatastrophic downside
  headの再走（範囲拡張は新規実験として可）
- EXP11: 低頻度騎手×調教師ペアという母集団選定（今回のas-of訂正版でも
  62%過大予測の再現は否定された）
- EXP12: O3（現行設計、mean系3特徴のplacebo近似）の同一設計での再走

**プロジェクト全体で既に確立された「死亡ルート」**（EXP01-12以前から
記録済み、詳細はMEMORY.mdの各`project_*`エントリ参照）: EVT分散特徴、
baba馬場適性収縮特徴、ELO（4アーム）、Glicko-2、レースレベルv1/v2、
血統SVD埋め込み、EV銘柄選抜、Benter二段、WIN5、三連単、券種×点数×金額
44,928セル総当たり等。これらも再走禁止のまま。

---

## 5. 再開に必要な新データ

現在のJRA表データ＋既存モデル基盤の範囲内では、上記の通り主要な既知
仮説を使い切った。再開の可能性がある方向:

- **真に新規の観測量**: 構造化された疾病・故障理由データ（現状存在
  しない、EXP10 DATA_AUDIT §3で確認済み）。パドック・映像・生体データ
  （JRA-VANガイドライン・利用規約上の制約も要確認）。
- **オッズ時系列そのものを対象とした価格形成研究**: 表データの「予測」
  ではなく、市場価格の**形成過程**（T-35→T-20→T-10→close間の価格
  ドリフト）を対象とする、質的に異なる研究方向。本ドキュメント§4-5で
  開始条件を規定する（現時点では開始しない）。
- **対戦相手ネットワークの再設計**: EXP12のplacebo失敗は「個体識別
  情報に価値がない」ことの完全な証明ではなく、「今回の設計・強度指標
  （EXP02動的能力）・集約方法では交絡を超える価値を示せなかった」と
  いう範囲に限定される。異なる強度指標・より厳密なplacebo設計を用いた
  再設計は理論的にはありうるが、新規の独立実験として、かつ十分な理由
  （今回のplacebo失敗をどう克服するかの具体的な設計変更）がない限り
  着手しない。

## 6. 前向き観測中の項目

- **EXP05-F**（表特徴の追加情報、前向き影武者観測）: 2026-09-19開始、
  目標6600レース。現状は収集基盤の検証段階（§3参照）。結果・性能は
  観測期間終了まで開封しない。
- **T-10自動馬券ライン**（`data/forward_prices/`、300 bet cohort、
  `docs/forward_price_protocol.md`）: 本番稼働中、独立した前向き検証
  プロトコル。
- **T-20サイトプレビュー**（`reports/site_odds/`）: 本番稼働中。

## 7. 本番接続済み／未接続

**本番接続済み**（EXP01-12の研究成果とは独立の、既存の本番運用ライン）:
- `unified_rank_v6.pkl` + `pl_calibrators_v6.pkl`（予測モデル本体）
- `compute_bets.py`（topdownエンジン、確率順候補→適応トリガミ床）
- T-10自動馬券ライン（`t10_runner.py`、JV-Linkオッズ取得→compute_bets→
  validate自動実行）
- T-20サイトプレビュー・T-4実投票（`masters_vote.py`）
- 静的サイト（`pycaliai-umami`、`build_site.py`）

**本番未接続**（EXP01-12はいずれも本番へ接続されていない）:
- EXP01-12の全ての研究成果。理由は各EXPの結論参照（Gate不合格・
  経済価値なし・仮説撤回等、いずれの経路でも「本番接続せず終了」）。
- EXP02は主Gate PASSだったが経済価値ゲートを満たさず未接続。
- EXP09はcoverage自体は再現したが本番接続していない。

## 8. 再利用可能な資産（研究基盤として残す）

- `pl_rank_distribution.py`（EXP10、Gumbel-max trickによる汎用PL順位
  分布計算エンジン、厳密DP+adaptive MC+全順列列挙、43テスト済み）
- `asof_features.py`（EXP11、日初固定as-of count計算+実データ削除不変性
  検証パターン、汎用テンプレート）
- `data/_research/mcond/exp02_features.parquet`（EXP02のdyn_skill_muが
  hid(文字列)+rid16+ban+dateで直接引ける形で保存済み、as-of能力lookupの
  即戦力資産）
- `data/elo_feats.parquet` / `data/glicko_feats.parquet`（既存ELO4アーム・
  Glicko-2、rid16+banで結合可能）
- `opponent_graph.py`の日初固定day-batchedアルゴリズム（EXP12、任意の
  as-of二部グラフ集約のテンプレート）
- `feedback_asof_population_definition`（メモリ、「低頻度」母集団定義の
  必須要件：as-of count・日初固定・削除不変性テスト・row数分離・
  full-period count分析禁止）
- `matchup_history.py`（対戦履歴ビルダー、現状表示専用だがグラフ構築の
  ビルディングブロックとして再利用可能）
- Gate/placebo/full-control設計パターン（EXP06-12で確立、meeting-day
  forward-chaining CV・開催日paired bootstrap・placebo検定の実装群）

---

## 9. ★2026-09-25 追記: EXP14 / EXP15 / EXP16A の状態と EXP16 の終了範囲

本節は §1 の状態一覧に対する追記である。EXP01〜EXP13 の記述・再実験禁止項目は変更しない。

| 実験 | 状態（2026-09-25） |
|---|---|
| EXP14 レジーム分離 / MoE | Stage 0 完了・Stage 1 未着手（手動条件分割は 3 件の独立実験で既に否定済み） |
| EXP15 Race-as-a-Set | **終了**。Context Gate FAIL（ΔLL は有意だが ΔBrier は非有意で 2 指標規則を満たさない）。R2・placebo は結果後に救済実行しない |
| **EXP16A Close-Market Residual Information Bound** | **Stage 1 完了**。spec v0.4 を commit `e7603678` で凍結し、rolling OOF 40 fit → 実方向 power audit → 2019-2023 crossfit 評価を実施 |

### EXP16A の結論（範囲を限定した記述）

- **Gate A（Q2a 対 terminal_close_market）= FAIL**: Δ −0.000015 nats/race、CI95 [−0.000239, +0.000207]、
  年方向 2/5、seed 方向 3/5、leave-one-year-out 全て CI 上限 > 0、placebo 未超過。
  正式文: 「単勝close市場に対するR0-cleanの残差情報を2019-2023 crossfitで検出できなかった。
  全券種・JRA全研究・馬券依存構造へ一般化しない」。
- **Gate B = NOT_APPLICABLE**（凍結 spec の run_if 未充足）。参考計算値は PASS-PRACTICAL だが
  **Gate として扱わない／再等級化しない**。
- **secondary finding**: 事前固定 8 残差特徴の Q2b−Q2a = −0.0036（CI [−0.004977, −0.002210]、5/5 年・
  5/5 seed、placebo 超過、2019-2022 約 −0.0040 → 2023 約 −0.0020 へ縮小）。
  **実務床 0.005 未満**のため **EXP16B として追試しない**。
  Q2b が存在するため「決済価格に対する情報はゼロ」とは書かない。
- **機構解釈**: within-experiment 対照では情報時点が主要な差であり、市場吸収が最も有力な解釈である。
  ただし、この実験だけで唯一の原因とは断定しない（pre と close では offset 分布・係数推定・較正難度・
  モデルミススペックが異なる）。「残る説明は市場吸収だけ」「因果的に証明した」「λ=1 を確定した」
  「遅い資金の 23% を予測した」はいずれも主張しない。
- **recovery_ratio 0.2327** は「historical_pre_snapshot から terminal close までの logloss gap のうち
  Q3a が回収した割合」だけを意味する。資金フロー（遅い資金・投票量・参加者・価格変動の予測精度）の
  割合ではない。
- **EXP05 との関係**: EXP16A の結果は、EXP05 で観測された判断時点の予測改善が締切までに市場へ反映され、
  決済価格に対する優位へ残らなかった可能性を支持する。ただし EXP05 の ROI 不改善を単一原因で
  説明したものではない。

### EXP16（Ticket Candidate Generation）の終了範囲

> 現在の R0-clean / Q1 と事前固定 8 残差特徴を確率源とし、単勝市場を起点に候補生成を最適化する経路は、
> terminal close 市場に対する実務床以上の増分情報を確認できなかったため終了する。

- 記載してよい: 「候補生成を工夫しても確率源の不足を自動的に補えない」。
- **一般化しない**: どの切り方でも +EV は出ない／JRA 全券種で +EV は不可能／他プールでも不可能／
  共同着順分布や払戻依存構造にも情報がない／新しい非表形式データでも不可能／
  局所的な市場非効率が存在しない。

### forward データ（T-35 / T-10 / close）

2026 年でも同じ時間構造が続くかを測る用途として**保持**する。
**2019-2023 の結果を forward で再確認するだけの作業は直ちに開始しない**。
必要観測数・最低期間・判定条件を先に設計する。

### 継続する禁止事項

価格形成モデルの本番接続、ROI 評価、候補馬券生成、資金配分最適化、production 変更、
2024/2025 の開封は引き続き行わない。

## まとめ

現在のJRA表データと既存モデル基盤（unified_rank_v6+市場データ）を使った
主要な追加予測仮説は、EXP01〜EXP12の体系的な敵対的検証を通じて使い切った。
これは「予測AI全体が不可能」という結論ではなく、**この基盤の上で合理的な
実装根拠がある仮説がなくなった**という状態整理である。次の研究段階は、
新規の観測量（オッズ価格形成、疾病・故障データ等）の確保と、それに基づく
新しい研究の開始条件の整理（本ドキュメント§4以降、および別途進行中の
オッズ時系列在庫監査・EXP05-F前向き観測レポート）にある。

EXP13以降は、新しい観測量・十分な標本・完全未開封期間が確保されるまで
開始しない。

関連: [[project_exp10_downside_risk]] [[project_exp11_hierarchical_bayes]]
[[project_exp12_opponent_network]] [[feedback_asof_population_definition]]
[[project_mcond_exp01_choice_dev]] [[project_mcond_exp02_dynamic_skill]]
[[project_mcond_exp03_latent_state]] [[project_mcond_exp04_invariant_info]]
[[project_mcond_exp05_market_residual]] [[project_mcond_exp05f_forward_shadow]]
[[project_mcond_exp06_jev_decision]] [[project_exp07_robust_portfolio]]
[[project_exp08_online_track_state]] [[project_exp09_conformal_abstention]]
[[project_unexplored_methods_audit_20260920]]
