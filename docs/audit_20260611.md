# PyCaLiAI 全領域監査 — 修正台帳（2026-06-11）

> 8 領域 × 30 エージェントの反証検証付き監査結果。critical/high は別エージェントが実コードを読んで検証済み（refuted=false）。
> **修正できた項目は `- [x]` にしてタイトルに取り消し線（`~~...~~`）を入れ、末尾に ✅ 完了メモを足す。**
> 全文ログ: `tasks/wejkyiwvl.output`（一時ファイル）/ 要点メモリ: `memory/project_full_audit_20260611.md`

凡例: 🔴critical 🟠high 🟡medium 🟢low ｜ 状態: `[ ]`未着手 `[~]`作業中 `[x]`完了

---

## 今日（次に weekly を回す前に・低リスク）

- [x] ~~🟠 **sync-hf.ps1 の `git checkout --force` が未コミット編集を破壊**~~ — `sync-hf.ps1`
  - ✅ 2026-06-11: checkout --force 前に tracked dirty を `git stash push` で退避し、switch back 後に `git stash pop`。-DryRun も元ブランチへ `checkout --force` で復帰するよう修正済

- [x] ~~🟠 **cowork_results.json が 5/26 で凍結**~~ — `weekly_post.ps1` / `generate_results.py`
  - ✅ 2026-06-11: ①weekly_post.ps1 の git add に `data/cowork_results.json` 追加（凍結の本丸）②generate_results.py の Cowork 集計失敗を `log.error`+traceback で可視化（黙殺解除）。残: 次回 Phase C 実行で generated_at が当日に更新されるか実地確認

- [ ] 🟠 **wide_kekka.csv が 5/17 停止 → ワイド的中が全敗計上** — `nicegui_app.py:840`
  - 5/23-24 の実的中 10 件（¥18,400）が損失表示。ワイドは bets の約1/3で P/L 画面が過小
  - 対応: 短期=wide_pays 空は集計外として注記表示（全敗より誠実）。恒久=weekly_post に wide_kekka.csv 更新を組込 + kekka最新日>wide_kekka最新日で警告バナー

- [x] ~~🟠 **見送りガードが fail-open**~~ — `weekly_nicegui.ps1`
  - ✅ 2026-06-11: validate exit 1（ガード実行不能=bundle/bets 不在・破損）を hard Fail 化。`-Force` でのみ明示バイパス（fail-closed）

---

## モデル層 — train/serve skew（最重要・critical）

- [~] 🔴 **v6 の 120 特徴中 30個が本番推論で死亡（serve skew）** — `export_weekly_marks.py` / `predict_weekly.py(parse_csv)`
  - ✅ 2026-06-11 ①定量化完了（serve_skew_eval.py / reports/serve_skew_eval.json）: 30列マスクで **test2024-25 ◎複勝圏 62.08%→57.53%(-4.55pt)・ECE複勝2.3倍/馬連1.8倍悪化**。本番は offline 天井62%に未達 = 4.5pt はバグ回収可能
  - ✅ グループ別内訳判明: **補正2個=+2.97pt(主因)** / hist_same4個=+1.03pt / 調教16個=+0.33pt / course・jockey・code≈0
  - ✅ ②-a 補正リネーム実装（export_weekly_marks.py、parse_csv後に 前走補正→prev_hosei / 前走補9→prev_hosei9）。定義は build_master_v2:70 と完全一致。20260607 で 0%→**カバレッジ36.2%復活**を確認
  - 残: ②-b hist_same(+1.03pt、生成元がbuild_master_v2に見当たらず要調査) / 調教(+0.33pt薄い) / course-jockey-code(≈0、見送り可) ③calibrator を serve 条件で再fit ④prev_hosei カバレッジ36%向上(make_weekly_hosei)で実効回収を最大化

- [ ] 🟠 **serve でも値が入る特徴の定義不一致** — `parse_kako5.py`
  - kako5_expected_good_count（カウント条件が学習=着順のみ/推論=人気&着順）、上り3F の1走ズレ、prev_pos_rel の分母（前走頭数vs今走頭数）、学習時100%NaNなのに推論時のみ実値
  - 対応: past_races 構築を学習・推論で単一正規化関数に統一

- [ ] 🟡 **前走走破タイムが '1.13.6' 文字列のまま to_numeric → 100%NaN（特徴が無言で死亡）** — `optuna_v6_marks.py`
  - utils.parse_time_str があるのに v6 学習パスで未適用。母馬も全行-9999
  - 対応: prep() で parse_time_str 適用。「coerce後NaN率100%の特徴があれば fail」アサーション追加

- [ ] 🟡 **本番モデルの学習データが 2022年末で凍結（3年以上停滞）** — `optuna_v6_marks.py`
  - LabelEncoder も train fit で未知カテゴリ率が単調増加。master_v2 は 2025-12-28 まであるのに2023-25未使用
  - 対応: シーズン前に split を1-2年スライドして再学習（train≤2024/valid=2025 で v7）。未知カテゴリ率を週次ログ化

- [ ] 🟢 **fillna(-9999) が LightGBM ネイティブ欠損処理を潰す** — `optuna_v6_marks.py`
  - 対応: NaN のまま lgb.Dataset へ渡す。心配なら missing indicator 列で A/B

- [ ] 🟢 **調教 JOIN の仕様不整合（14日制限なし・馬名JOIN）** — `build_master_v2.py`
  - 対応: merge_asof に tolerance=14日。可能なら血統登録番号JOIN。CLAUDE.md と実装の as-of 規約を一致

---

## 確率キャリブレーション

- [ ] 🔴 **compute_bets が PL joint + 券種別calibrator を捨て素朴近似で連系EV計算** — `compute_bets.py:174`（賭け金の本丸）
  - ワイド=p_sho_i×p_sho_j独立積（実測+21〜27%過大、floor0.80が実質0.61-0.66）、オッズ=馬連/3
  - pl_calibrators_v6 の wide/umatan calibrator は本番マネーパスで未使用。正確版は backtest_pl_ev.py の all_wide_mat に実装済み
  - 対応: bundle に印馬ペアC(5,2)=10組の calibrated p_wide/p_umatan を埋込、compute_bets はそれを読むだけに
  - 📝 2026-06-11: docstring にワイドEV独立積近似の系統過大を明記。本体（PL joint埋込）は未着手（要設計・最優先級）

- [ ] 🟠 **calibrator の fit が valid=2023 に永久固定（ドリフト無防備）** — `build_pl_calibrators.py:51`
  - 四半期再実行しても同じ2023に再fitするだけ。2026 live も入らない。p_win≥0.4帯で既に+1.6pt過信
  - 対応: calibrator だけ rolling window（fit=直近2年/eval=直近6ヶ月）に。isotonicは単調なので順位評価を汚さない

- [ ] 🟡 **valid=2023 の三重使用**（early stopping + HP選択 + isotonic fit）— `optuna_v6_marks.py:327`
  - 対応: valid を時系列で前半/後半に割る、or rolling fit へ移行

- [ ] 🟡 **ECE_high_p が単一ビンの符号付きバイアス（過信と過小が相殺）** — `optuna_v6_marks.py:240`
  - 対応: 高p域を3-5ビンに分割した加重|gap|平均に。名称も aggregate_bias_high_p に

- [ ] 🟡 **bundle の p_win/p_sho(calibrated) と p_plc(raw) 混在で単調性もΣ整合も崩れスキーマ文書と矛盾** — `export_marks_json.py:124`
  - 対応: renta calibrator 追加 + p_win≤p_plc≤p_sho 単調クリップ。docs/marks_schema.md を実態に修正

- [ ] 🟢 **同着の的中ラベルを argsort で任意に3頭切り詰め** — calibrator fit ラベルにノイズ
  - 対応: `jyun<=k` ベースに統一（共通関数化）

- [ ] 🟢 **audit_marks.py の ECE が等幅10ビンで複勝/馬連は実質2-3ビン** — v6採用根拠の物差しが弱い
  - 対応: joint_calibration_v6.py と同じ log寄り/分位ビンに統一して再計測

---

## 目的関数・Optuna

- [ ] 🟠 **「5-fold CV」が実質フルvalid一括評価、best と2ndの差0.0042はノイズ水準** — `optuna_v6_marks.py:273`
  - 上位trialのalphaは0.03〜0.27に散在＝統計的に区別不能なものからargmax選択
  - 対応: fold std をログし mean-1×std で選択。上位5trial×3seed で再現幅測定。「5-fold CV」表記を実態に修正

- [ ] 🟡 **最終retrainが best_iter×1.1 で early stopping なし → 保存モデルがtrial評価と別物** — `optuna_v6_marks.py:406`
  - 対応: trialモデルをそのまま保存 or 保存直前に composite/ECE 再計測して JSON 併記

- [ ] 🟢 **印付けの composite重み配分（深さ5系に35%）が実投入額と未検証にズレ** — `export_marks_json.py:80`
  - 対応: all_trials ログで重み±50%振っても argmax 不変を確認しドキュメント化

---

## 時系列・ドリフト

- [ ] 🟡 **test(2024-25) が実験判定に反復消費され dev set 化** — `exp_*.py` 9本
  - 対応: 選択/WIN判定は valid、test は採用後1回のみ。test を 2024/2025 に分割し2025を封印セット化

- [ ] 🟡 **exp_recency_sweep の REAL_SMALL_GAIN は valid ノイズ過適合の疑い**（同数値で ewm は CEILING と矛盾）
  - 対応: hl=1.8 折込み保留。seed3つ+test年別スライスで再確認。verdict文言を ewm 基準と統一

- [ ] 🟡 **build_strategy_walkforward の「walk-forward」が in-sample 予測ベース** — `build_strategy_walkforward.py`
  - 2018-22 の安定性が学習済みデータROIで水増し。MIN_VALID_RACES=8 は小さすぎ
  - 対応: expanding-window再学習 or docstring/CLAUDE.md に「in-sampleで真のWFでない」明記。MIN_VALID_RACES↑

- [ ] 🟡 **elo/glicko/odds_traj のリーク検査が構成上必ずPASSの自己言及型** — `build_elo_feats.py:106`
  - 対応: 「過去走だけでゼロから再計算して記録値と一致」型 assert に置換

---

## ベッティング層・市場効率（ROI の本丸）

- [ ] 🟠 **compute_bets が T-10ライブオッズ未配線 + bets.json書込未実装 + --dry死亡** — `compute_bets.py:259`
  - live_odds参照0件、書込コード0件、jvlink は0B31のみ（ワイド0B33/馬単0B34パーサ不在）
  - 対応: --live-odds-dir読込（race_id一致でオッズ上書き、ok=false/古いは見送り）、bets.json in-place merge（.bak退避）、--dry分岐。spec乖離中はdocstringに「未実装」明記
  - 📝 2026-06-11: docstring に未実装（T-10未配線/bets.json書込未実装/--dry死亡）を明記し運用者の誤認を防止。本体配線は未着手（要設計）

- [ ] 🟠 **券種ポリシー（単勝boost1.6・馬連全廃）が30bets旧スナップ由来、更新データで逆転** — `compute_bets.py:196`
  - 単勝120.8%(CI[53%,243%]控除率と区別不能)→更新445betsで96.3%、廃止馬連71.5%>複勝68.4%。CI評価0件
  - 対応: 券種別集計に Wilson CI(的中率)+bootstrap CI(ROI)。「CI下限が控除率超」のときだけ方針変更する規律をコード化。実績数値は cowork_results.json から自動参照

- [ ] 🟠 **Benter blend 棄却が ≤2023 fit の in-sample汚染（最優先の伸びしろ）** — `stage1_benter_blend.py:231`
  - fit データの実質100%がモデル/calibrator の in-sample。audit_market_layer.json 自身が α=0.3 で test R²改善(FLAG_f_helps)検出済
  - 対応: α,β を valid2023のみ/OOFで再fit。残れば p_blend=softmax(α·logf+β·logπ) を compute_bets の EV に接続。※収益化ゲートはまだ80%未満なので過度な期待は禁物

- [ ] 🟠 **ワイドEV二重近似でEVフロア無効化**（calibration項と重複）— `compute_bets.py:174`
  - 上の「PL joint捨て」と同根。all_wide_mat 流用で厳密joint計算

- [ ] 🟡 **「高EVほど実現ROI低下」なのに賭け金はEV増加関数、穴overlay単勝はキャップ無し** — `compute_bets.py:68`
  - audit_ev_bin_roi: 単勝EV1.1-1.3がピーク81.1%、2.0+は64.4%。50+オッズ帯はROI30-61%/CLV-54〜67%
  - 対応: amount_for_ev を「補正後期待ROI帯→金額」に。EVCalibrator(ev_filter.py、実装済み未接続)接続。穴overlay単勝に上限

- [ ] 🟡 **「Kelly」が実態どこにも未実装（dead code、実走はedge比例配分）** — `backtest_pl_kelly.py:55`
  - 対応: 名称/docstring に「edge比例（Kelliではない）」明記。中期=レース内同時Kelly（解析解、数十行）

- [ ] 🟡 **shape/見送り閾値の統計的根拠が薄い（パーセンタイル基盤12バンドルのみ）+ pct()フォールバックが壊れる** — `compute_bets.py:31`
  - 対応: 9時オッズで2024-25リプレイしshape別ROI測定。pct()フォールバックは None→fail-safe見送りに。chaos_quantiles を全シーズンで再生成

- [ ] 🟢 **EVスケール定義がモジュール間で3種混在（/0.80・素のp×odds・100円単位）** — `ev_filter.py:150`
  - 対応: 「EV=p×払戻倍率(1.0=損益分岐)」を標準として docs に明記、各docstringに換算式併記

---

## 抜け漏れ（完全性チェッカー指摘）

- [ ] 🟠 **取消・除外の「返還」処理が結果集計に皆無** — `generate_results.py:388`
  - 取消馬絡みの買い目が全額損失計上で券種別ROIが系統的に過小。同着も get_top2 が frozenset() で無条件外れ
  - 対応: kekka の着順非数値行=取消検出、該当馬含む買い目は「返還」で投資/払戻両方から除外。同着も的中側に

- [ ] 🟠 **bets.json の内容妥当性チェック皆無** — `validate_cowork_bets.py`
  - 馬番実在・券種ホワイトリスト・金額単位/予算上限・重複が未検証。三連単(廃止済)settlementが現存し素通し
  - 対応: ①馬番∈bundle.horses ②券種∈{単,複,ワイド,馬単}（三連単reject）③100円単位&0<amt≤MAX_BET&レース合計≤予算 ④重複なし。違反は fail-closed

- [ ] 🟠 **空・激減バンドルでも Phase A が成功して HF push（品質ゲート不在）** — `export_weekly_marks.py:311`
  - parse_csv が想定列数以外を無警告で捨てる、n_done=0でも bundle 書出し exit0
  - 対応: export 末尾に sanity check（race数比率/オッズ被覆率/p_win非null率）で閾値割れなら非0 exit。weekly_nicegui で race_count 0/激減なら push 停止

- [ ] 🟡 **本番/rollback pkl のバックアップ態勢なし** — `build_pl_calibrators.py:166`
  - in-place 上書きで utils.backup_model 未使用。v5 rollback pkl は git未追跡のローカル1コピーのみ
  - 対応: dump 直前に backup_model 必須化。v5 pkl を git(LFS)/クラウド退避。master_v2/外部データのバックアップ明文化

- [x] ~~🟡 **穴overlay と shape が同一買い目を重複生成（dedup なし）**~~ — `compute_bets.py`
  - ✅ 2026-06-11: push() で (kind,sel) を統合（既存があれば boost を max 採用して return）。20260607_bundle で動作確認、重複なし・exit 0

---

## ソフトウェア品質・運用

- [ ] 🟠 **本番ライン(v6 stack/compute_bets)のテスト0 + venv311にpytest未導入で既存テストも実行不能** — `tests/`
  - 対応: venv311にpytest導入。compute_bets の純関数(pct/allocate/amount_for_ev/compute_race_bets)にゴールデンテスト。weekly_nicegui に pytest 1行

- [x] ~~🟡 **weekly_nicegui -Post が weekly_post.ps1 の失敗を無視して sync-hf 実行**~~ — `weekly_nicegui.ps1`
  - ✅ 2026-06-11: weekly_post.ps1 の後に `if ($LASTEXITCODE -ne 0) { Fail }` で伝搬。残: Phase C 終了時の generated_at 当日確認サマリは未実装

- [ ] 🟡 **旧8モデルアンサンブルがデフォルト毎週実行 + Phase C集計が pred CSV に構造依存** — `weekly_nicegui.ps1:213`
  - 対応: デフォルトを Skip 反転（-WithPredict で opt-in）。generate_results は pred CSV 不在でも Cowork集計だけで完走できるよう FileNotFoundError→警告+空DF

- [ ] 🟡 **apply_encoders が21ファイルにコピペ定義（学習/推論一致が暗黙依存）** — 本番4本
  - 対応: utils.py/encoders.py に1実装集約、本番4本(train_unified_rank/backtest_pl_ev/build_pl_calibrators/build_payout_curve)を import に

- [ ] 🟡 **COL_RACE_ID が16ファイル重複定義、3本は旧名 "レースID(新)" のまま** — `stacking.py:56` 他
  - 対応: COL_RACE_ID/COL_BAN/COL_JYUN を utils.py に一元定義。旧名3本は再学習時に正名へ

- [~] 🟡 **モデルパス設定が三重構造（グローバル書換+環境変数+デフォルトv1）で再現性低** — `backtest_pl_ev.py:33`
  - ✅ 2026-06-11 部分: export_weekly_marks の --model デフォルトを v5→v6 に変更（手動実行の事故防止）
  - 残: backtest_pl_ev.py のグローバル mutation+環境変数オーバーライド廃止（パス引数化）は未対応

- [ ] 🟡 **requirements.txt が全行未固定かつ実環境と乖離（再構築不能）** — `requirements.txt`
  - torch/nicegui/pyarrow 未記載、記載のpytestは未導入。pkl互換は lightgbm/sklearn バージョン依存
  - 対応: `pip freeze > requirements-lock.txt`。requirements.txt に上限ピン(lightgbm==4.6.*等)

- [ ] 🟢 **ルート直下157本のPythonに本番と実験が混在（CLAUDE.md記載93本から+64）** — 整理方針が未運用
  - 対応: untracked実験(exp_*/gate*/physics_*等)を analysis/experiments/ へ。ルートは「weekly_nicegui/run_v*_pipeline から参照されるもののみ」

- [~] 🟢 **設定の時限断線: sync-hf.ps1 の「2026」ハードコードが2027に黙って同期停止** — `sync-hf.ps1`
  - ✅ 2026-06-11 部分: hosei/training の年パターンを `H_20[0-9]{2}...` に変更（2027 以降も同期継続）
  - 残: 見送り閾値の data/betting_gates.json 単一ソース化（validate/compute_bets/cowork_prompt の3重定義）は未対応

- [ ] 🟢 **結果系JSON/CSVの非アトミック上書き（クラッシュで破損ファイルがcommit）** — `generate_results.py:1035`
  - 対応: utils.py に write_json_atomic（tmp→os.replace）追加し results/cowork_results/bets/live CSV に適用

- [ ] 🟢 **文字化けパスのゴミファイル `E：PyCaLiAIreports_bundle_marks_tmp.json`（直近も再生成）** — パス連結ミスの痕跡
  - 対応: 内容確認→発生源(外部プロセスのパス指定)修正→削除→.gitignore

---

## NiceGUI フロント

- [ ] 🟡 **lru_cache の invalidation 不整合（parse_kekka が date キーのみで None/旧内容を恒久キャッシュ）** — `nicegui_app.py`
  - 対応: bundle/kekka の mtime もキャッシュキーに

- [ ] 🟡 **重い同期I/O・描画が asyncio ループ上で実行、操作中に全クライアント固まる** — `nicegui_app.py`
  - 対応: 重い処理を run.io_bound/cpu_bound に。アクティブタブのみ遅延描画

- [ ] 🟡 **bundle不在の日付に切替えると前の日付のレースパネルが残留** — `nicegui_app.py`
  - 対応: 日付切替時にコンテナ .clear()

- [ ] 🟡 **weeklyパーサ・脚質分類・成績集計が3ファイルに重複（4357行モノリス）** — `nicegui_app.py`/`build_course_stats.py`/`app.py`
  - 対応: pycali_common.py に集約して import。ファイル分割は後

- [ ] 🟢 **bundle/bets.json の破損が「不在」と同じ表示で原因究明できない** — `nicegui_app.py:179`
  - 対応: except でパースエラーをログ。UIも「存在するがパース失敗」を不在と区別

- [ ] 🟢 **Cowork出力/CSV文字列を HTMLエスケープせず ui.html に埋込（公開Space）** — `nicegui_app.py:4093`
  - 対応: html.escape() ヘルパで text系フィールドに一律適用

---

## 良い点（維持）

- リーク衛生: merge_asof(backward, allow_exact_matches=False)、rolling の shift(1)、Elo/Glicko の pre-race 記録 — 全箇所正しい
- 実験規律: 事前固定 verdict 基準、CEILING_CONFIRMED まで一貫した反証、v7失敗の組織知化
- 監査ハーネス: joint_calibration_v6/eval_rps_brier/audit_ev_bin_roi/audit_market_layer の自己反証体制
- pl_probs.py: 不変量self-test付き厳密PL
- validate_cowork_bets: LLM逸脱へのコードガードという設計判断
